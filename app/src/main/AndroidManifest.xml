<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.arjava.moviefavoritcp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <permission android:name="com.arjava.moviefavoritcp.READ_DATABASE" android:protectionLevel="normal"/>
    <permission android:name="com.arjava.moviefavoritcp.WRITE_DATABASE" android:protectionLevel="normal"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DetailsMovie"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".activity.SearchActivity"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".activity.ActivitySearchNav"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".activity.TopRatedActivity"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".activity.AboutActivity"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".activity.FavoriteActivity"/>
        <provider
            android:authorities="com.arjava.moviefavoritcp"
            android:name=".provider.MovieProvider"
            android:exported="true"
            android:readPermission="com.arjava.moviesfavorite.READ_DATABASE"
            android:writePermission="com.arjava.moviesfavorite.WRITE_DATABASE"/>
    </application>

</manifest>